vich_uploader:
    db_driver: orm

    mappings:
        image_nom:
            uri_prefix: '%app.path.image_nom%'
            upload_destination: '%kernel.project_dir%/public%app.path.image_nom%'
            # generating unique name for images
            namer: Vich\UploaderBundle\Naming\UniqidNamer
        newsletter_documentPDF:
            uri_prefix: '%app.path.newsletter_documentPDF%'
            upload_destination: '%kernel.project_dir%/public%app.path.newsletter_documentPDF%'
            # generating unique name for pdf
            namer: Vich\UploaderBundle\Naming\UniqidNamer
        event_documentPDF:
            uri_prefix: '%app.path.event_documentPDF%'
            upload_destination: '%kernel.project_dir%/public%app.path.event_documentPDF%'
            # generating unique name for pdf
            namer: Vich\UploaderBundle\Naming\UniqidNamer

